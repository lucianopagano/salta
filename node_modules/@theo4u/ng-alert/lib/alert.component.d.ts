import { EventEmitter } from '@angular/core';
import { IMessage, CloseType } from './interfaces';
export declare class NgAlertComponent {
    dismissable: boolean;
    rawMessage: IMessage;
    messageChange: EventEmitter<{}>;
    closeType: CloseType;
    closeTypes: typeof CloseType;
    message: IMessage;
}
